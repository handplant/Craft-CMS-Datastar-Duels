{% fragment %}
  <div id="alert-reset"></div>
{% endfragment %}

{% set resetResponse = datastar.runAction('users/send-password-reset-email', {
  loginName: signals.loginname
}) %}

{% if resetResponse.isSuccessful %}
  {% patchelements %}
    <div id="form"></div>
  {% endpatchelements %}
  {% patchelements %}
    <div id="info" class="space-y-4">
      {#      {{ dump(resetResponse) }} #}
      <p>
        🔐 The Key Must Be Reforged!
      </p>
      <p>
        A password may be lost, but not all is forgotten — you have requested to reclaim access to your {{ systemName }} account.
      </p>
      <p>
        To reforge your key and regain entry, follow the link we’ve just sent to your email address.
      </p>
      <p>
        👉 Check your inbox now!<br>
        Inside, you’ll find the path to restore your access and continue your quest.
      </p>
      <p>
        📬 Didn’t get the message?<br>
        Even the strongest spells can be misdirected — check your spam folder, just in case.
      </p>
    </div>
  {% endpatchelements %}
{% endif %}
